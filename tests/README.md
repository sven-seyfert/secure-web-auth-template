## Tests

### App logic

- API-based
  - See the simple curl tests in `./api-based.sh`.
- UI-based (frontend)
  - Test manually via the web UI.

### Backend logic

Typical test files like `*_test.go` are located next to their package files. For example, `routes.go` is accompanied by `routes_test.go`.
